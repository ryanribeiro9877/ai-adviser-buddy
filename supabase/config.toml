# 05/08/2026 (CODE 1.1): era "ghlitpgdwurrwbsmkagb" — projeto que NAO e o Gestor nem o
# JurisAI e nao aparece na org deste login. Deploy sem --project-ref ia para la. Hipotese
# Lovable (ai-adviser-buddy / build-your-dreams-153) nao verificada; o projeto nao esta
# na lista desta conta. O valor abaixo e o unico certo: gestao_marketing.
project_id = "gzjwnjdpxpbmdhcyefvs"

# 04/08/2026 (GT-13) - upload-midia estava com verify_jwt=true no projeto e um deploy meu com
# --no-verify-jwt mudou isso para false. O flag da CLI so sabe DESLIGAR; sem uma declaracao aqui
# o valor anterior nao volta sozinho.
# 11/08/2026 - cron horario escoar_imagens chama esta edge com x-mcp-key (a propria edge
# autentica via mcp_key_valida). Mesmo padrao de drive-audio-transcribe / pipeboard-metrics-sync:
# gateway aberto, autenticacao dentro. verify_jwt=false e obrigatorio para o cron nao depender
# de JWT de anon/service_role no agendador.
[functions.upload-midia]
verify_jwt = false

# ESP-12: chamada interna pelo cron usa x-mcp-key e a propria edge valida via mcp_key_valida.
[functions.drive-audio-transcribe]
verify_jwt = false

# 13/08/2026 (auditoria) - estas duas JA estavam com verify_jwt=false no projeto, mas sem
# declaracao aqui: exatamente a armadilha descrita no bloco do upload-midia acima. Um
# `supabase functions deploy` destas sem esta linha aplicaria o DEFAULT true e o portao
# passaria a recusar os chamadores por x-mcp-key antes de a edge autenticar.
# Ambas autenticam dentro, via mcpKeyValida (_shared/mcp_auth.ts) - verificado por chamada
# sem chave: mcp-server responde JSON-RPC -32001 e meta-actions responde
# 401 chave_ausente_ou_curta.
[functions.mcp-server]
verify_jwt = false

[functions.meta-actions]
verify_jwt = false

[functions.traffic-chat]
verify_jwt = false

[functions.traffic-agent-job]
verify_jwt = false

[functions.meta-health]
verify_jwt = false

# ditado do chat (Bearer do usuario) E chamada interna da drive-audio-transcribe (x-mcp-key,
# sem Authorization). Precisa continuar com o gateway aberto - a propria edge autentica.
[functions.transcribe-audio]
verify_jwt = false

# Fase A: cron autentica por mcp_key_valida dentro da edge.
[functions.pipeboard-metrics-sync]
verify_jwt = false

# 14/08/2026 — coleta estrutural oficial (campaigns/adsets/ads) via Pipeboard.
# Crons autenticam dentro da edge por x-mcp-key.
[functions.pipeboard-structure-sync]
verify_jwt = false

# 14/08/2026 — leitura ao vivo do Pipeboard (somente get/list/search/...).
# traffic-chat e ops autenticam por x-mcp-key / Bearer; a edge valida mcp_key_valida.
[functions.pipeboard-read]
verify_jwt = false

# 05/09/2026 - coleta dos recortes (idade/genero/plataforma) que a Windsor deixou de trazer em
# 13/08. Cron autentica por x-mcp-key dentro da edge, igual as outras duas do Pipeboard. Sem
# esta linha o deploy nasce com verify_jwt=true e o gateway devolve
# UNAUTHORIZED_NO_AUTH_HEADER antes de a edge ver a chave — foi o que aconteceu na primeira
# publicacao desta funcao.
[functions.pipeboard-breakdowns-sync]
verify_jwt = false

# Sonda de identidade (somente leitura). Sem esta linha o deploy nasce com verify_jwt=true
# e o gateway rejeita a x-mcp-key antes de a edge validar — a propria edge autentica por
# mcp_key_valida, como as demais.
[functions.meta-identity-probe]
verify_jwt = false

# Resolve keys Meta do preset Jurídico Salvador–BA (cache one-shot). MCP auth na edge.
[functions.geo-preset-resolve]
verify_jwt = false

# ESP-37: traffic-chat chama com x-mcp-key; a propria edge autentica via mcp_key_valida.
[functions.gerar-legendas]
verify_jwt = false

# 22/08/2026 — chat/job chamam com x-mcp-key; a propria edge autentica.
[functions.compliance-check]
verify_jwt = false

[functions.waba-template-create]
verify_jwt = false

[functions.waba-template-replicate]
verify_jwt = false

# ESP-30: monitor de expiracao/escopo dos tokens Meta. Cron chama com x-mcp-key; a propria
# edge autentica via mcp_key_valida. Mesmo padrao de bm-monitor/meta-identity-probe.
[functions.meta-token-monitor]
verify_jwt = false

# 21/08/2026 — crosscheck Graph (Edge Secrets) x Pipeboard para COHAPM/Legal. So leitura.
[functions.meta-token-crosscheck]
verify_jwt = false

# ESP-41: entrega do digest por e-mail + dreno de alertas criticos. Cron e trigger chamam com
# Bearer get_mcp_api_key(); a propria edge autentica via mcp_key_valida.
[functions.enviar-digest]
verify_jwt = false

# GT-24: chamada exclusiva do painel autenticado. O gateway valida o JWT e a edge
# ainda confirma getUser + papel admin antes de revelar contas ou gravar vínculos.
[functions.integration-verify]
verify_jwt = true

# 14/08/2026 — job hibrido de Recomendacoes da IA (SQL detecta + LLM redige/valida).
# Cron 09:35 UTC autentica via x-mcp-key; gateway JWT aberto.
[functions.traffic-reco-job]
verify_jwt = false

# 29/08/2026 — inspect/register de numeros WABA (Cloud API). Auth na edge (x-mcp-key).
[functions.waba-phone-connect]
verify_jwt = false

# 31/08/2026 — sonda Drive (SA + pastas compartilhadas). Auth na edge (x-mcp-key).
[functions.drive-access-probe]
verify_jwt = false
