# Espelhos SQL

Cópias **fiéis, versionadas em git** de migrações/objetos já aplicados no banco
Supabase do projeto `gestão_marketing` (`gzjwnjdpxpbmdhcyefvs`), aplicadas via
`apply_migration` (MCP) e que não têm arquivo correspondente em
[`../migrations/`](../migrations).

> **Não são migrações a rodar.** Todos os arquivos começam com o aviso
> "espelho para git — NÃO re-executar" (os objetos já existem em produção).
> A CLI do Supabase só lê `supabase/migrations/`, então nada aqui é aplicado
> por `supabase db push`. Servem como registro histórico e de auditoria: o
> cabeçalho de cada arquivo conta a decisão, a validação e o contexto.

Há dois tipos de arquivo aqui:

- **`<timestamp>_<nome>.sql`** — o SQL **exato** que rodou, extraído de
  `supabase_migrations.schema_migrations`. O nome corresponde à versão
  registrada no banco.
- **`espelho_*.sql`** — espelho escrito à mão, consolidando uma ou mais
  migrações com o contexto da decisão. Fiel ao efeito, não necessariamente
  caractere a caractere.

> ⚠️ **Esta pasta não é uma história reconstruível.** `../migrations/` só tem as
> três migrações de 20/07; tudo depois disso foi aplicado via `apply_migration`
> e existe apenas aqui. Como os espelhos pressupõem objetos criados por
> migrações ausentes, **não dá para recriar o banco do zero** a partir deste
> repositório — nem movendo estes arquivos para `../migrations/` (a cadeia
> quebraria: p.ex. `20260728144316` altera `agent_context`, que nenhum arquivo
> versionado cria). Para reconstruir, a fonte é o histórico de migrações do
> próprio Supabase.

| Arquivo | Fase | Conteúdo |
|---|---|---|
| `espelho_proposals_ciclo_f1.sql` | F1 | `v_custo_proposta` v2 (métrica oficial vs. rateio) + `security_invoker` + dormência das tabelas de propostas (decisão de escopo). |
| `espelho_targets_f13.sql` | F1.3 | Tabela `targets` (metas & tetos), índices, RLS e seed vigente. |
| `espelho_evaluate_alerts_v2_f13.sql` | F1.3 | `evaluate_alerts` v2 — R1 (CPL) passa a ler o teto vivo de `targets`. |
| `espelho_sec_revoke_anon_f13.sql` | F1.3 | Hardening: revoga `execute` de funções SECURITY DEFINER de escrita para `anon`/`public`. |
| `espelho_regra_3_dias_f2.sql` | F2 | Regra dos 3 dias (recomendação de pausa por criativo) — `evaluate_alerts` v3 + seed da regra. |
| `espelho_winners_f22.sql` | F2.2 | Detector de criativos vencedores — `evaluate_winners` (grava em `ai_recommendations`) + cron. |
| `espelho_r6_r7_f23.sql` | F2.3 | Regras R6 (queda de entrega) e R7 (orçamento) — `evaluate_alerts` v4 + seeds. |
| `espelho_chat_tables_f31.sql` | F3.1 | Tabelas do chat "Operação" (`chat_conversations`, `chat_messages`) + RLS. |
| `espelho_f3_anexos_audio.sql` | F3 | Coluna `chat_messages.attachments` (metadados de anexos multimodais + transcrição de áudio). |
| `espelho_f33_actioncards.sql` | F3.3 | ActionCards — aditivo em `approval_requests` (conversation_id), `audit_log` imutável e função `decide_approval`. |
| `espelho_f4_execucao_meta.sql` | F4.1/F4.2 | Execução real na Meta — `meta_execution_config` (flags 3 camadas + dry_run), `set_meta_execution_config`, colunas `executed_at`/`execution_result` + campanha cobaia de teste. |
| `espelho_f43_f44_compliance_bm.sql` | F4.3/F4.4 | Tabela `compliance_rules` versionada (16 regras FIN/CRI/LGL) + RLS; contexto das edges `compliance-check` e `bm-monitor`. Seed resumido — enunciados completos só na migração do Supabase. |
| `espelho_migracoes_27-07-2026.sql` | 27/07 | Telemetria do turno (`chat_messages.diagnostico`), Realtime em `chat_messages`, watchdog de frescor (`check_data_freshness`, SQL puro sem edge) + cron 09:45. |
| `20260728133731_limpa_agent_context_e_cria_agent_style.sql` | 28/07 | Neutraliza números de estado na memória (causa raiz da alucinação de 27/07), remove jargão interno e cria `agent_style` — formatação fora do prompt, editável sem redeploy. |
| `20260728140410_add_conhecimento_breakdown_effect_e_gates.sql` | 28/07 | Memória institucional: breakdown effect, triagem antes de interpretar número, fase de aprendizado, formato obrigatório de recomendação e sazonalidade do consignado. |
| `20260728141940_add_expiracao_24h_approval_requests.sql` | 28/07 | Expiração de aprovações em 24h — `expires_at` + `expire_stale_approvals` (vencido vira `rejected` com nota, para não mudar front/executor) + cron horário. |
| `20260728142936_prepara_acoes_de_criacao_meta.sql` | 28/07 | Preparação das ações de criação — enum `adset`, flags novas (OFF), lista branca `contas_permitidas_criacao` e teto de sanidade de orçamento. |
| `20260728144316_isola_agent_context_por_empresa.sql` | 28/07 | Isola a memória institucional por empresa — `agent_context.company_id` (NULL = fato universal) + reclassificação dos fatos existentes. |
| `20260728151211_cria_agent_knowledge_com_temas_criticos.sql` | 28/07 | Cria `agent_knowledge` (base consultável, progressive disclosure via `get_conhecimento`) com os temas `otimizacao` e `unidade_economica`. |
| `20260728172944_isola_execution_config_por_empresa_e_valida_conhecimento.sql` | 28/07 | `meta_execution_config` deixa de ser singleton (config por empresa — bloqueante antes de ligar flags) + prazos de revalidação e `check_conhecimento_validade` semanal. |
| `20260728180208_remove_espelho_crm_dash_fora_de_escopo.sql` | 28/07 | **CRM sai do escopo:** derruba `lev_leads`/`lev_propostas`/`lev_sync_state` (LGPD — minimização), `get_funil_credito` vira stub de escopo, fatos de CRM desativados. Sem CAC por contrato pago daqui em diante. |
| `20260728194745_f55_alertas_tier_quality_waba.sql` | F5.5 | Monitor de tier/qualidade dos números WhatsApp — `evaluate_waba_tier_alerts` (SQL puro) + dedup. **Superado pelo fix `…194905`.** |
| `20260728194905_f55_fix_cast_alert_severity.sql` | F5.5 | Fix: `alerts.severity` é enum — cast `::alert_severity` no INSERT. Versão vigente de `evaluate_waba_tier_alerts`. |
| `20260728195029_f54_f55_relatorio_diario_secao_waba.sql` | F5.4/F5.5 | Seção WhatsApp no relatório diário (08:30 BRT) — `post_daily_report` com números por tier/qualidade e cobertura declarada. **Superado pelo fix `…195151`.** |
| `20260728195151_f54_fix_relatorio_waba_cobertura_e_nome_template.sql` | F5.4 | Fix: exclui `phone_external_id = ''` da contagem de cobertura (ausência de dado ≠ zero) e busca o nome do template via join. Versão vigente de `post_daily_report`. |
| `20260728210046_chat_jobs_select_policy_e_realtime.sql` | Subagentes | Pré-requisito do front: policy de SELECT em `chat_jobs` (`is_company_member`, escrita segue exclusiva do `service_role`) + `chat_jobs` no Realtime. |
| `20260729122851_f56_tabela_infobip_dispatches.sql` | F5.6 | Cria `infobip_dispatches` — ingestão dos exports Excel da Infobip (grão = linha; `service_name` distingue Outbound/Inbound/MAU), RLS + dedup por `(message_id, service_name)`. `price_raw` gravado como vem, sem converter. |
| `20260729122954_rpc_get_weekly_report_data.sql` | 29/07 | `get_weekly_report_data` — métricas do relatório semanal (investimento, formulários, custos, CTR, conversão, quebra por campanha). Declara no retorno o que **não** é coletado (demografia) em vez de omitir. |
| `20260729125229_infobip_dedup_nulls_not_distinct.sql` | F5.6 | Dedup no banco: unique passa a `NULLS NOT DISTINCT` (PG15+), então NULL colide com NULL. Aplicado com a tabela vazia. |
| `20260729130350_f56_hardening_service_name_not_null.sql` | F5.6 | Segunda camada do mesmo furo: `service_name` vira NOT NULL com default `'-'`. Complementa o `NULLS NOT DISTINCT` acima (defesa em profundidade). |
| `20260729142700_f53_flag_criar_template.sql` | F5.3 | Flag `criar_template` nasce **OFF** nas duas empresas, antes da edge existir — evita o default-aberto de sempre. |
| `20260729143155_f53_tabela_waba_template_creations.sql` | F5.3 | Cria `waba_template_creations` — fluxo rascunho (redator LLM + guardião fail-closed, nunca submete) → submit (3 camadas + rate limit) → watch. Escrita exclusiva da edge. |
| `20260729193223_m07_historico_config_campanhas.sql` | M-07 | `campaign_config_snapshots` + `snapshot_campaign_config` (cron 09:25) + `get_alteracoes_config` — torna executável a doutrina "você não é o único ator". Foto 1x/dia: mudança feita e revertida no mesmo dia é invisível, e a leitura devolve essa limitação junto do resultado. |
| `20260729134405_rpc_get_report_export_data.sql` | 29/07 | `get_report_export_data` — fonte única da exportação rica do relatório (série diária, campanhas, top 15 anúncios, tetos vigentes). `security invoker`, complementa `get_weekly_report_data`. |
| `20260729202134_g02_g03_g05_estado_operacional_contas_e_wabas.sql` | G-02/03/05 | Estado operacional por conta (`integrations.estado_operacional`: ativa/nao_operacional/**quarentena**, que vence flag de empresa) e por WABA (`wabas.estado_local`: ativa/legada) + gate `conta_elegivel_para_acao` (deny por padrão). |
| `20260730115819_notificacoes_realtime_e_rpc_pendencias.sql` | 30/07 | Realtime + `replica identity full` em `approval_requests`/`alerts` e `get_notificacoes_pendentes` (fonte única do sino; notificação é projeção de estado, não entidade). **Superado pelo fix `…132744`** — esta versão filtrava `status::text = 'pendente'` e zerava as aprovações em silêncio. |
| `20260730132744_fix_get_notificacoes_pendentes_rotulo_enum.sql` | 30/07 | Fix: comparação passa a ser **tipada** (`status = 'pending'::approval_status`) em vez de `::text` — rótulo errado agora falha alto na criação da função em vez de devolver zero. Versão vigente de `get_notificacoes_pendentes`. |
| `20260730175943_fix_get_criativos_conteudo_isolamento_empresa.sql` | 30/07 | **Vazamento entre empresas:** `get_criativos_conteudo` varria `ads` sem filtro de `company_id` (peça da COHAPM entrava no portfólio de crédito da Legal). Nova assinatura com `p_company_id` obrigatório + legada mantida com aviso e campo `empresa` por item. A edge migrou no `traffic-chat` v28 (verificado em produção em 30/07: nenhuma chamada à legada; Legal 67 criativos, COHAPM 35, zero contaminação) — a legada segue viva só por compatibilidade e pode ser aposentada. |
| `20260730194848_job_v2_checkpoint_e_segmentos.sql` | Job v2 | `chat_jobs.checkpoint` + `segmento` — o worker respeita o teto de parede (~400s) vivendo em até 3 segmentos: grava checkpoint e reinvoca a edge, retomando do ponto exato. Estado no banco, não na memória do worker. |
| `20260730204732_paginacao_get_criativos_conteudo.sql` | Job v2.1 | Overload com `p_offset`/`p_limit` (ordem determinística: gasto desc, nome) + `total`/`exibidos`/`offset`/`restantes`. Fecha a lacuna em que o aviso de corte mandava "pedir um recorte mais estreito" sem que existisse parâmetro de recorte — 26 de 30 legendas ficavam invisíveis sem caminho de recuperação. |

| `20260731135336_upload_midia_tabela_e_flag.sql` | Upload mídia v1 | `media_uploads` (Drive → biblioteca da conta Meta, dedup por `(drive_file_id, account)` com `nulls not distinct`) + flag `upload_midia` nascendo **OFF** nas duas empresas. Subir mídia não gasta nem publica, mas é escrita na conta: respeita master, dry_run, lista branca de contas e teto por hora. |

| `20260731165022_drive_midia_analises.sql` | Job v2.2 | `drive_midia_analises` — veredito visual por mídia do Drive (produto aparente, texto visível, riscos, aproveitável sim/não/incerto). Chave por arquivo **+ versão**, então cada rodada analisa só o que falta ou mudou. `base_da_analise` declara o que foi visto (miniatura, nunca o vídeo interno). |

| `20260803121910_fix_integrations_status_nao_nasce_conectada.sql` | 03/08 | **"Conectada" falsa vinha do schema:** `status` DEFAULT `'connected'` + `connected_at` DEFAULT `now()` faziam toda linha nascer conectada sem nenhuma chamada à Graph (22 de 22 `connected` desde sempre, 2 sem `external_id`). Defaults passam a `nao_verificada`/`quarentena`, `connected_at` sem default, CHECK de vocabulário e **trava estrutural** `status <> 'connected' OR external_id preenchido` — o banco recusa a mentira em vez de depender do front. |

| `20260803141646_gt05_gt27_escopo_fatos_e_contrato_ativacao.sql` | GT-05/GT-27 | Escopa a memória por empresa (globais vigentes 20 → 12; nenhum global cita mais consignado/INSS/CLT/teto) e reescreve dois fatos: categoria especial agora é gravada **por construção** pela `meta-actions` v4.1, e o contrato de ativação passa a dizer que **aprovar card é o ato de executar** — não existe mais "o card fica pendente". ⚠️ **Espelho resumido:** descreve os 4 blocos, mas o SQL exato vive só em `schema_migrations.statements[1]`. |

| `20260804153429_gt06_mensagem_declara_estado_da_flag.sql` | GT-06 | `pode_executar_acao` passa o estado da flag para o **texto** da mensagem: o dado existia só em campo estruturado que o chat não repassava — informação que vive num lugar só, que ninguém lê, não existe. Flag ligada em ação que a executora não executa ganha aviso próprio. |
| `20260804164810_gt09_campos_de_config_e_unidade_em_centavos.sql` | GT-09 | Coleta de configuração vai pelo `meta-campaign-status` (lê a lista da Graph, enxerga campanha pausada); Windsor descartado por só devolver campanha **com entrega** — cobriria 2 de 29. Princípio: configuração é estado, não métrica. |
| `20260804172753_gt10_semente_do_publico_declarada.sql` | GT-10 | Semente dos públicos semelhantes declarada pelo gestor: os "Leads Convertidos" que semearam a família LAL 1/2/3/5% são **CLT apenas** — quatro percentuais da mesma semente. |
| `20260804174302_gt44_tres_flags_decisao.sql` | GT-44 | Decide as três flags que não executavam nada. `replicar_template` **removida**: o `audit_log` prova replicação aprovada pela Meta com a flag em `false`, ou seja, ninguém a lia. Manter seria fingir um controle que não existe — pior que não ter controle. |
| `20260804175120_drive_pastas_monitoradas_e_plano_de_varredura.sql` | Drive | Pastas monitoradas viram **dado**, não segredo: o sistema lia um único id vindo de `DRIVE_CRIATIVOS_FOLDER_ID`, então acrescentar pasta exigia deploy. Acesso amplo não é cobertura. |
| `20260804180552_cron_drive_watch_0845.sql` | Drive | Cron 08:45, registrado **depois** de provado (200, 5,6s, zero LLM). Antes do `windsor-sync` das 09:00 de propósito: peça nova entra no espelho antes da coleta de métrica. |
| `20260804185957_drive_chave_inclui_base_da_analise.sql` | Drive | Chave única passa a incluir `base_da_analise`. Sem isso a reanálise autorizada (áudio + vários quadros) era impossível: o arquivo não muda, então o incremental que economiza custo virava trava — 89% dos vídeos tinham ficado "indeterminado" olhando **um quadro**. |
| `20260804192159_drive_convencao_de_base_evidencia_e_criterio.sql` | Drive | Convenção de nome da base inclui a versão do critério. As 67 análises foram feitas 2h11 **antes** do deploy que acrescentou "educação financeira" e "segurança" à lista de produto — e a distribuição confirma: zero peças nesses dois temas. |
| `20260804193832_drive_liberacao_e_do_arquivo_nao_da_analise.sql` | Drive | A liberação do gestor é atributo **da peça**, não da linha de análise — senão quem consultar a base nova vê veredito de máquina sem camada humana. |
| `20260804193925_drive_sincroniza_liberacao_contra_a_fonte.sql` | Drive (fix) | Conserto: o UPDATE filtrava `IS NULL`, mas as linhas nasciam `FALSE` pelo default — cinco peças **afirmavam** que o gestor não liberou o que ele liberou, e a conferência procurava NULL e reportava zero. Sincronizar derivada compara com a **fonte** (`IS DISTINCT FROM`), nunca procura um valor suposto. Default removido: NULL = sem decisão, FALSE = recusa. |
| `20260804194310_drive_liberacao_derivada_tambem_no_update.sql` | Drive (fix) | Conserto: o gatilho de herança cobria só INSERT, mas o pipeline grava com `ON CONFLICT DO UPDATE` — no caminho de UPDATE ele não disparava. Coluna derivada tem de ser derivada nos dois caminhos, senão "derivada" é só intenção. |
| `20260804202255_teto_sanidade_3000_e_aviso_de_orcamento.sql` | 04/08 | Teto de sanidade cai de R$ 5.000 para **R$ 3.000**/dia + função que traduz o orçamento pedido no que ele realmente permite: a Meta declara que diário é **média**, com folga de ~75% num dia isolado e garantia só no semanal (R$ 60/dia → até R$ 105 no dia, R$ 420 na semana). |
| `20260813161229_fecha_anon_em_rpcs_definer_e_tabelas_de_credencial.sql` | Segurança 13/08 | Auditoria: com **só a chave publishable** (que é pública por design), uma chamada não autenticada em `mcp_keys_prontidao` devolvia **HTTP 200 com dado interno**. Eram 12 funções `SECURITY DEFINER` alcançáveis por `anon`, 7 delas capazes de escrever. Este arquivo fechou as **tabelas** (`revoke all ... from anon`) e fixou `search_path` em **44** funções; a parte de função foi **no-op** — ver linha seguinte. |
| `20260813161344_fecha_anon_revogando_de_public_correcao.sql` | Segurança 13/08 (fix) | Conserto: o grant nunca estava em `anon`, estava no pseudo-papel **`PUBLIC`** (ACL `=X/postgres`), que `anon` herda — revogar de um papel não remove o que vem de `PUBLIC`. E a primeira verificação por HTTP **pareceu** confirmar o conserto: 10 das 12 deram 404, que era `PGRST202` (argumento faltando na chamada de teste), não permissão. Quem denunciou o no-op foi o catálogo — `has_function_privilege('anon', …)` seguia `true`. Lição: **status HTTP não prova autorização; o catálogo prova.** |

| `20260903200454_camada_de_conhecimento_deterministico.sql` | Camada determinística | `moldes_de_resposta` (registro de moldes em 3 classes) + `resolucoes_de_molde` (telemetria da fronteira determinístico/LLM) + `moldes_a_revalidar` (governança). `revalidar_ate` vencido **para de emitir** em vez de emitir com aviso: conhecimento fixo e errado é pior que geração variável, porque é consistentemente errado e soa autoritativo. |
| `20260903200709_camada_de_conhecimento_deterministico_seed.sql` | Camada determinística | Os 14 moldes. Motivo medido: as 13 `perguntas_ouro` têm **39 execuções com resposta real** (3 por código, todas em 06/08) e saíram **39 respostas distintas** — zero repetição byte a byte; em 10 dos 13 códigos o conjunto de números citados mudou entre rodadas do mesmo dia (PO-01 rodou 3x em 32 min: as de 18:00 e 18:07 citaram R$ 1.512,00 como pior dia, a de 17:35 não citou esse valor em lugar nenhum). **Ressalva:** as 3 rodadas são versões diferentes do agente, então a divergência de *número* está confundida com mudança intencional — a de *texto* não. Determinizável **~5%** por turno inteiro, **~48%** por segmento; não 90%. Os 6 textos canônicos são duplicados byte a byte no fallback de `_shared/resposta_canonica.ts`, conferidos por hash sha256 na prova. |
| `20260903220000_composicao_hibrida_do_bloco_canonico.sql` | Camada determinística | Composição **híbrida**: o bloco canônico é concatenado por código (`_shared/composicao_hibrida.ts`) e a análise em volta segue gerada. Coluna `moldes_de_resposta.composicao` classifica os 14 moldes em `turno_inteiro` (1: `SIS_SONDA_OK`), `segmento_componivel` (7: os 6 calculados menos `EST_ROTULO_RASTREIO`, mais os 2 de ato) e `nao_componivel` (6: as 5 recusas + `EST_ROTULO_RASTREIO`) — o default é o restritivo, porque liberar análise em volta deve ser decisão explícita. Critério: um molde é componível quando texto livre depois dele **não consegue tornar o canônico falso** nem repeti-lo; ressalva anexada a uma recusa é lida como permissão, e apontar vencedor contradiz o gabarito que diz que sem rótulo de rastreio não se aponta vencedor. `resolucoes_de_molde.caminho` passa a aceitar `hibrido` e ganha `chars_canonicos`/`chars_gerados` (NULL, não zero, quando a parte não existe); `proporcao_canonica_medida()` calcula a proporção **real** em produção, para confirmar ou desmentir a estimativa de ~48%. O bloco vai sempre no **início** da mensagem: é a resposta, e o gestor às vezes lê por voz — bloco no fim faria ele esperar a leitura de ~1.900 chars de análise para ouvir se o card saiu. |

| `20260903240000_origens_reais_dos_campos_dos_moldes.sql` | Camada determinística | **As origens declaradas não existiam.** O seed gravou `campos[].origem` com nomes de chave *plausíveis* que nenhuma RPC devolve: `custo_llm_periodo.total_usd` (a chave é `custo_usd`), `custo_llm_periodo.turnos` (é `chamadas`), `avaliar_orcamento_diario.soma_programada` (é `exposicao_atual_por_dia`, e a RPC nem é ferramenta de chat), `saude_das_integracoes.com_coleta_7d` (**não existe janela de 7 dias**) e `campaigns do company_id` (não há ferramenta que liste campanhas — quem declara as contagens é `get_overview`). Origem que mente é pior que origem ausente, porque a coluna existe para a auditoria responder "de onde veio este número?" sem ler código. Agora ela grava a string **exata** que `_shared/valores_do_molde.ts` lê, e `_prova_valores_do_molde.ts` [4] compara os dois lados: mexer em um sem mexer no outro reprova. **Dois gabaritos mudaram de conteúdo, por motivos diferentes.** (a) `ATO_CONFIRMACAO_CARD.pendencia` e `ATO_CARD_NAO_EMITIDO.proximo_passo` saíram: não são dado, são análise, desenhados quando a camada era binária e o molde tinha de responder o turno inteiro — no híbrido a análise mora fora do bloco, e obrigatórios que nenhuma ferramenta preenche seriam molde morto. (b) `EST_SAUDE_INTEGRACOES` afirmava "coleta medida nos últimos **7 dias**" e a RPC não tem janela de 7 dias: tem `dias_tolerancia`, que é *parâmetro* e vale 3 por padrão — emitir "7" lendo 3 é o pior defeito possível aqui (número certo com rótulo errado, travado e autoritativo), então o gabarito passou a citar a tolerância como valor. **5 dos 7 componíveis têm extrator**; `NUM_EXPOSICAO_ORCAMENTO` (nenhuma ferramenta devolve exposição vigente) e `NUM_CUSTO_LLM_PERIODO` (0 chamadas em produção, forma não conferível) ficaram sem, e por isso `instrucaoDeComposicao` passou a **exigir extrator** — sem esse portão, ligar a camada faria esses dois mutilarem a resposta em todo turno. |

| `espelho_classificacao_derivada_e_atribuicao.sql` | Crons 03/09 (cont.) | **Classificação derivada da configuração + atribuição de marca + veredito da transcrição.** Decisão das 3 migrações `20260903233000`–`20260903235000` (SQL em `../migrations/`). As 6 campanhas ativas estavam com `category` nula e as 3 regras de custo exigem `category in ('leadgen','mensagem')` — mortas. Em vez de devolver a classificação ao gestor, foram **derivadas**: `base_de_resultado(categoria, optimization_goal, objective)` já era a doutrina canônica do repo, e as 29 campanhas classificadas à mão concordam com ela sem contraexemplo. O ponto fino: `OUTCOME_ENGAGEMENT` **não decide sozinho** (aparece em `mensagem` e em `engajamento`); quem separa é `CONVERSATIONS` na meta do conjunto — derivar só pelo objetivo seria o erro que aponta a regra para a régua errada. Trava de neutralidade: a derivação só é aceita se `base_de_resultado(derivada,…) = base_de_resultado(null,…)` em **todos** os conjuntos, o que a torna incapaz de mudar o resultado de quem lê essa função hoje (15 de 15 neutras) e explica por que `get_report_export_data` não muda. Regressão contra as 29 humanas antes de gravar: **24 acertos, 5 abstenções, 0 contradições**. Causa consertada com 2 gatilhos (a campanha entra antes dos conjuntos, então um resolve o que o objetivo decide e o outro fecha quando a meta chega); manual nunca é sobrescrita. O teste do gatilho pegou defeito **crítico**: `case when tg_table_name='campaigns' then new.id else new.campaign_id end` — em PL/pgSQL o CASE resolve os campos dos dois ramos, então **todo insert de campanha** falharia e o espelho da Meta pararia. Instagram: esgotados promoted_object, UTM, url de destino, permalink, título, corpo, targeting (genérico e idêntico), conta, página e company_id — **não há caminho que não passe pelo nome**, então virou alerta próprio com `linha_produto = 'nao atribuida'` explícito, nunca chutado. (`waba_phone_numbers.verified_name` **é** caminho legítimo sem nome, mas cobre 1 de 6.) Corrigido erro meu da entrega anterior: o vigia chamava `trafego` de lacuna e a ação sugerida mandaria classificar **errado**. Ponto cego: de 3 regras × **0** campanhas para 3 × **3**. Susto registrado: o primeiro `evaluate_alerts` acusou "0 lead" em campanhas com 83/34/23 conversas (~R$ 4,00/conversa) porque R4 olhava `campaigns.leads`, coluna inconsistente — o outro agente corrigiu o denominador na mesma janela e a reexecução **encerrou** os 3 alertas enganosos. As duas mudanças só entregam valor juntas. |
| `espelho_crons_e_alertas_legiveis.sql` | Crons 03/09 | **Crons que rodam de verdade + alerta legível.** Espelho de decisão de 13 migrações (SQL completo em `../migrations/`, 9 arquivos `20260903200000`–`20260903208000`). Diagnóstico: o `pg_cron` marca sucesso ao **enfileirar** o `net.http_post`, não ao completar — 17 dos 28 jobs eram desse tipo, então "sucesso" em série não provava entrega; e `net._http_response` **não guarda a URL** (só `id`/`status_code`/`content`/`timed_out`/`created`), então nem achando um 500 dava para saber de qual cron era. Sem trilha, sem conserto durável: daí instrumentar antes. Nasceram `tarefas_agendadas` (catálogo onde cada tarefa declara a **pergunta operacional** que responde — cron sem pergunta é enfeite) e `execucoes_agendadas`, que separa **`sucesso_vazio` de "não rodou"** — antes os dois eram silêncio, e é por isso que cron parada não era percebida. A amarração passou a ser pelo `request_id` (que é nosso) + contagem do destino antes/depois, então 2xx com `ok:false` no corpo conta como **falha** e edge que declara 50 mas grava 0 não passa. `vigiar_tarefas_agendadas` transforma ausência em alerta e substitui `check_data_freshness` (fixada numa empresa, apontando para a `windsor-sync` aposentada). Alerta ganhou padrão único com `onde`/`quanto`/`acao`/`janela`/`linha_produto` + `padrao_versao` para conviver com o histórico; `evaluate_alerts` parou de fazer `delete`+reinsert (que zerava `created_at` e apagava a idade do alerta, justamente o que decide prioridade) e virou idempotente por `chave_dedupe`, com `vistas` e `primeira_deteccao`. Contaminação: as três marcas dividem **um** `company_id`, então filtrar por empresa não separa linha — a linha sai do token `[LEV]`/`[LAF]` cruzado com `brand_identity.marca_tag`, e quando não resolve o alerta **diz** que não resolveu. Defeito achado pela própria instrumentação: duração de 300s com HTTP 200 e `timed_out=false` era o intervalo da conferência, não a tarefa (`now() - iniciado_em`, fechado por cron de 5 min); passou a usar `_http_response.created` — **299996 ms → 917 ms** na mesma tarefa. |

| `espelho_regua_de_custo_por_conversa_por_marca.sql` | Réguas 03/09 | **Régua de custo por conversa, separada por marca.** Decisão das migrações `20260903235500` e `20260903235600` (SQL em `../migrations/`). Depois que `dd12675` deu um denominador único ao custo por resultado, o número virou confiável e o buraco apareceu: **não existia régua de `custo_por_conversa`** — o motor caía em `coalesce(teto_vigente, alert_rules.threshold)` e julgava conversa contra os R$ 21,80 da regra `cpl`, que é régua de formulário. Aquele 21,80 é o **p75 de março/2026 das oito campanhas `[SALT]` de La Felicità, outra agência** — ou seja, o número "genérico" que governava três marcas era o p75 de **uma** marca, de uma operação extinta; Jurídico (R$ 4,00) e Sistema Ocular (R$ 4,96) passavam como "dentro do limite" medidos contra o alvo alheio. Réguas aplicadas: Jurídico **R$ 7,00**, La Felicità **R$ 26,00**, Sistema Ocular **R$ 12,00**, as três `provisoria=true` com `revisar_em=24/09/2026` — cinco dias fechados (dois, na VISTTA) descrevem nível, não dispersão, e isso vale para as três, não só para a recente. O ponto fino: a dispersão **diária** de La Felicità (13,18 a 89,19) não mede custo, mede **contagem pequena** (1 a 12 conversas/dia; no dia de R$ 89,19 houve **uma**) — régua tirada dali ficaria em R$ 60-90 e nunca dispararia; a janela de 3 dias da mesma marca varia só 1,6x contra 6,8x do diário, e é ela que R1 e R5 comparam. VISTTA ganhou régua provisória em vez de ficar sem porque **"sem régua" não era silêncio**: sem régua ela caía nos R$ 21,80 alheios — a escolha real era entre número declarado provisório e número errado que ninguém sabe que está lá. Dois níveis (atenção/crítico) **já existiam**, não como dois números mas como duas janelas sobre o mesmo: R1 `high` no acumulado da campanha, R5 `critical` no criativo três dias seguidos — R5 estava **morto** para conversa (exige `teto is not null`) e foi religado. Doutrina respeitada: não nasceu caminho novo, `teto_vigente` foi **estendido** (`teto_vigente_da_marca` resolve marca > empresa > `targets`; a versão de 2 args delega com marca nula e, em conversa, devolve `teto_que_governa` nulo — igual a antes — mas agora com `reguas_por_marca` declarando as três, em vez de negar que existam). Fallback genérico removido **só** na base `conversas`: ausência de régua vira ausência de veredito. Ajuste sem migration por `definir_regua_de_marca` (versão anterior fica com `vigente=false`; recusa marca fora de `brand_identity`, porque régua com acento diferente nunca seria encontrada e falharia calada). Prova: 0 de 3 campanhas disparam (-42,8% / -35,1% / -58,6%), **mas a linha não está morta** — 9,7% dos anúncio-dia do Jurídico e 14% dos de La Felicità ficam acima da própria régua, o que não acontece é três dias seguidos; e o back-test em março/2026 pega as 2 piores campanhas (R$ 32,00 e R$ 26,60) e libera as 2 saudáveis (R$ 24,22 e R$ 14,44). Sem conflito com `spend_no_leads`, e é **estrutural**: R1 exige resultado > 0, `spend_no_leads` exige resultado = 0, os dois lendo o mesmo `resultados_da_base`. |

| `espelho_transcricao_com_destino_e_escoamento.sql` | Áudio 03/09 | **Transcrição ganha destino, depois o acervo escoa.** Decisão das migrações `20260903250000`–`20260903253000` (SQL em `../migrations/`) e de duas versões da edge `drive-audio-transcribe`. A pergunta que chegou era de custo; a resposta foi outra: o impedimento nunca foi preço — `get_acervo_para_anuncio` **carregava** `transcricao_audio` na CTE e **não a emitia**, então 19 transcrições prontas nunca chegavam a quem escolhe peça. Saída antes do gasto. Estado do áudio virou conceito único (`estado_do_audio_da_peca`) porque o contador media transcrição por texto vazio e assim classificava vídeo **ouvido e sem locução** como fala não avaliada — o sistema pedia ao gestor pendência inexistente. A distinção que o booleano apagava: **`sem_fala` é resposta, `falha_tecnica` é lacuna.** Truncagem de 110 chars decidida por medição (mediana ~180, máx ~600 chars) com texto inteiro quando a chamada filtra por `p_drive_file_ids`; `cobertura_de_audio` vai no **topo** porque `traffic-chat` tem lista branca por item e não pode ser tocada nesta frente. O escoamento travou em 33 de 172 com HTTP 546: a edge baixa o vídeo inteiro para memória e morria **depois** de baixar, então um `.mov` de banco de imagens de **705,4 MB** na pasta "Brutos" derrubava a corrida e bloqueava os pequenos atrás — inclusive os do Jurídico, que são o material de compliance. Guarda de 60 MB antes do download (empírica: maior que passou tem 55,2 MB) com rótulo próprio. Final: **145 transcrito · 23 sem_fala · 4 grandes demais · 0 pendente**, por **~US$ 0,15** contra estimativa de US$ 0,41–0,59 (a estimativa usava tamanho como proxy de duração e errou para cima). **Achado de compliance:** a primeira comparação áudio×tela deu "zero risco" e era falsa — lia `->>'aprovado'`, chave que a função não devolve, e NULL virava aprovação; o primeiro controle positivo também não disparou porque as regras são regex sensíveis à ordem. Com o portão comprovadamente vivo (3 bloqueios no controle, 0 no neutro): 0 bloqueios no áudio, 5 na tela da Legal é Viver. Mas as 10 regras são todas de **oferta de crédito**, e o Jurídico da COHAPM são **42 vídeos com áudio, 28 com a tela vazia** — 42 prometem "análise jurídica gratuita", 19 acusam prática abusiva, 8 afirmam direito **incondicional** a benefício que na lei é condicional. Nada casa nenhuma regra porque não há regra para publicidade jurídica: **o risco não está no áudio contra a tela, está na régua.** Preço registrado em `model_prices` (US$ 1,25/1M áudio) — quase entrou 3,00, que é a tabela da **Realtime API**, não a de transcrições. |

| `espelho_regua_juridica_e_portao_fail_closed.sql` | Compliance 04/09 | **Régua jurídica em aviso + portão incapaz de aprovar por omissão.** Decisão das migrações `20260904100000`–`20260904120000` (SQL em `../migrations/`) e de 2 edges. A pergunta era o tamanho da exposição: **zero** das peças de teor forte está no ar, e nenhuma virou anúncio um único dia — as 6 peças do Jurídico que entregam agora são de tom condicional ("os juros *podem* ser abusivos"), 2 dias no ar, R$ 76,25. A resposta muda a urgência, mas não o risco: o teor mais forte ficou fora por **acaso operacional, não por controle**. O negativo foi provado antes de ser reportado — não há FK entre peça e anúncio, e "nenhuma no ar" vindo de junção quebrada tem a mesma aparência de boa notícia; a via `approval_requests.payload->>'drive_file_id'` → `execution_result->>'id_criado'` → `ads.external_id` liga 123 de 123, enquanto casar nome de arquivo com nome de anúncio liga **0 de 42**. Sinal canônico: `ads.status='ACTIVE'` carrega `effective_status` da Meta (inclui `ADSET_PAUSED`/`CAMPAIGN_PAUSED`), então pausa de pai já vem embutida — conferido, 97 de 97 com pai ativo. **Nada foi pausado.** Régua: 5 regras (`LGL-JUR-01`..`05`) ancoradas no **Provimento CFOAB 205/2021** (conferido vigente em 04/09; revisão "em fase final" desde dez/2025 mas nada publicado), CED art. 39, Lei 8.906/94 art. 34-IV e CDC 31/36/37 — **todas em `atencao`, nenhuma bloqueia**, porque `checar_par_texto_e_peca` só recusa em `reprova` e régua larga ligada sem advogado trava a operação de manhã. Escopo foi obrigatório, não cosmético: as versões ingênuas pegavam "o Registrato do BC é **gratuito**" e "**cuidado** com boleto falso" da Legal é Viver — 6 falsos positivos **medidos**, não imaginados. Dois remédios distintos: 4 das 5 ancoram em lookahead `(?=.*(juridic|advogad|judicial))`, e a `LGL-JUR-03` usa **proximidade** (`[^.!?]{0,25}` entre "análise/consulta" e "gratuit") — é a proximidade, não o assunto, que separa "análise jurídica gratuita" de "serviço gratuito". A solução estrutural (coluna `escopo`) foi recusada por mexer em tabela com 6 consumidores vivos. A dúvida grande fica declarada em vez de escondida: `LGL-JUR-03` pega **42 de 42**, e o art. 3º-I veda gratuidade "*como forma de captação*" — condicional que a frase não tem; e se tráfego pago já é captação ativa (art. 2º-VIII), o problema é o **canal, não a palavra**. Nem eu nem o gestor somos advogados. **Terceiro episódio do mesmo defeito**: a comparação áudio×tela lia `->>'aprovado'`, chave inexistente, e NULL virava aprovação — quem desmentiu foi controle positivo, e o método virou instituição. `checar_promessas_proibidas` agora **levanta exceção com zero regra ativa** (martelo deliberado: operação travada é melhor que operação sem portão) e devolve `bloqueia`/`regras_consideradas`; `provar_portao_de_compliance()` roda controle positivo (3 famílias de regra, para não morrer junto com uma) **e negativo** (portão histérico acaba desligado por quem tem pressa), com vigia diária virando alerta crítico. Nas edges: `gerar-legendas` tratava erro da RPC mas **não resposta vazia sem erro** — `parData` nulo dava veredito `""`, que não contém "reprov", e a variante saía **apta**; `meta-actions` comparava por igualdade com "reprova", então vocabulário novo passaria calado. Ambas viraram lista de liberados. Removida a leitura `compl?.aprovado !== false`, onde a ausência valia **verdadeiro**. `_prova_portao_fail_closed.ts` distingue o que libera do que é código morto: `=== true` sobre chave inexistente é inofensivo e não é acusado, `!== false` é. Item menor: `usage` da OpenAI passa a ser gravado em `transcricao_usage` (jsonb, porque `input_token_details.audio_tokens` é aninhado e o contrato tem variante por duração) — a apuração de US$ 0,15 foi **derivada** de caracteres-por-segundo, e a próxima será medida. Sem backfill: o `usage` das 145 já feitas foi descartado na resposta e só voltaria transcrevendo de novo. |

**Sobre as três assinaturas de `get_criativos_conteudo`** (todas vivas no banco): `(boolean)` é a legada sem filtro de empresa, mantida só por compatibilidade e sem consumidor desde o `traffic-chat` v28; `(boolean, uuid)` é a que a edge do chat usa; `(boolean, uuid, int, int)` é a paginada, usada pelo `traffic-agent-job` v2.1.
